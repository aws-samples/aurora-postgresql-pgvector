# Minimal requirements for Bedrock Similarity Search Notebook
# Only the absolute essentials - leverages existing SageMaker/Jupyter packages

# AWS SDK for Bedrock access
boto3>=1.43.40

# PostgreSQL vector database
psycopg>=3.3.4
pgvector>=0.4

# Parallel processing for faster embedding generation
pandarallel>=1.6.0

# Note: pandas, numpy, ipython, jupyter are already installed in most environments
# Note: setuptools, packaging, and other system packages are typically pre-installed
numpy>=2.1.0,<3
